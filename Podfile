# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'TestNetworkViewer' do
  pod 'Alamofire', '~> 4.5'
  pod 'SwiftyJSON'
  pod 'SVProgressHUD'  

  # Pods for TestNetworkViewer

  target 'TestNetworkViewerTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'TestNetworkViewerUITests' do
    # Pods for testing
    inherit! :search_paths
  end

end
